# Supabase
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Stripe
VITE_STRIPE_PUBLISHABLE_KEY=""

# Tracking (opcional — fallback se não houver em system_settings)
VITE_GTM_ID=""
VITE_PIXEL_ID=""

# Como usar:
# 1. Copie este arquivo para .env (cp .env.example .env)
# 2. Preencha com as chaves do projeto (não comite o .env)