/**
 * Shared Components Index
 * 
 * Reusable "Lego" components used across the application.
 * Import from '@/components/shared' for consistency.
 */

// Address input with CEP auto-complete
export { AddressFields, type AddressData } from './AddressFields';

// Unified image upload for assets
export { AssetImageUpload } from './AssetImageUpload';

// Empty states for tables and lists
export { EmptyState, type EmptyStateType } from './EmptyState';

// Error boundary wrapper for tables
export { TableErrorBoundary } from './TableErrorBoundary';
